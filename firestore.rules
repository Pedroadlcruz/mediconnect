rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public access for now to enable testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}